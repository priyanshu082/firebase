import {auth,provider} from "../config/firebase"

export const Login =()=>{
    return (
        <div>
            <p>Sign in with Google To Continue</p>
            <button>Sign In</button>
        </div>
    )
}